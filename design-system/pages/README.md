# Page overrides

Place page-specific deviations from `../MASTER.md` here as `<page>.md` (e.g. `dashboard.md`).  
If a file exists for the page being built, its rules override MASTER for that page only.
