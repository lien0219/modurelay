name: Restore audit snapshot

on:
  push:
    branches:
      - feature/usage-detail-privacy
    paths:
      - .github/workflows/restore-audit-snapshot.yml

permissions:
  contents: write

jobs:
  restore:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/usage-detail-privacy
          fetch-depth: 0

      - name: Restore exact tracked snapshot from develop
        run: |
          git fetch origin develop
          git checkout origin/develop -- frontend/audit.json
          rm -f .github/workflows/restore-audit-snapshot.yml
          git diff --check

      - name: Commit restoration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: restore tracked frontend audit snapshot"
          git push origin HEAD:feature/usage-detail-privacy
